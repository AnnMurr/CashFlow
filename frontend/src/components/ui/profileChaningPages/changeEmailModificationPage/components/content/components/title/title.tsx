import { FC } from "react";
import { Inner } from "./styledTitle";

export const Title: FC = () => {
    return (
        <Inner>
            <h5>
                Enter new email address
            </h5>
        </Inner>
    )
}