import { FC } from "react";
import { Container } from "./styledTitle";

export const Title: FC = () => {
    return (
        <Container>
            <h2>
                Sign Up
            </h2>
        </Container>
    )
}